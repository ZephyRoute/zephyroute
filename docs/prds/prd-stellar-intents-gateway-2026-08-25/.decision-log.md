# Decision Log — PRD: Stellar Intents Gateway

Canonical memory and audit trail for this PRD run. Every decision, change, and override is recorded here as the conversation unfolds.

- **2026-08-25** — Workspace bound at `docs/prds/prd-stellar-intents-gateway-2026-08-25/`. No prior `.stellar-build/bmm/config.yaml` existed in a project; `{project-root}` set to a newly created `~/stellar-intents-gateway/` folder since this is the first artifact of an ongoing project (per user's master rules, this project spans future R&D, not a one-off). `{project_name}` inferred as `stellar-intents-gateway`. Global config's `communication_language: English` overridden for this run — user has consistently written in Spanish throughout prior sessions on this project, so responses continue in Spanish.
- **2026-08-25** — Intent: Create (no existing PRD for this project). Existing inputs identified: (1) prior "Intents Gateway Live Test" artifact (Fase 2, live technical validation of NEAR Intents → Stellar → DeFindex), (2) "Integrate First, Build Last" artifact (Fase 3, re-evaluation against the project's 10 master rules, 13 questions answered, published at https://claude.ai/code/artifact/01a3f145-f24f-4e82-9609-e6ea7c054f95). Both to be extracted as source inputs during Discovery rather than re-derived from scratch.
- **2026-08-25** — Stakes calibration: **Launch**. This is a real Mainnet/SCF Integration Track candidate with real users and capital at stake — PRD written at full depth (Success Metrics, NFRs, Constraints, Risk register, Integration/Dependencies).
- **2026-08-25** — Working mode: **Fast path**. Two subagents (artifact extraction + competitive/market research) ran in parallel during Discovery; three consolidated fast-path questions were then asked (monetization, whether SCF is an explicit v1 goal, launch timeline) rather than a full section-by-section coaching walkthrough.
- **2026-08-25** — Monetization: integrator fee automático de NEAR Intents habilitado por defecto en v1 (no custom billing code). Exact rate unknown — Open Question 9.
- **2026-08-25** — SCF Integration Track application is an explicit primary goal of v1, not just background context — Success Metrics (§9 of prd.md) were written to double as the on-chain metric a panel would ratify for tranche #3.
- **2026-08-25** — Target launch timeline: weeks (aggressive), consistent with Mainnet-From-Day-One — contingent on closing Phase 2's 5 pending confirmations first, now folded into prd.md §14 Open Questions.
- **2026-08-25** — Full draft of `prd.md` and `addendum.md` written from the extraction + research subagent digests plus the 3 fast-path answers. Per Fast-path convention ("you review and we iterate"), the Finalize sequence (decision-log audit, input reconciliation, reviewer gate, polish, close) is deliberately deferred until after the user reviews this draft, rather than run immediately.
- **2026-08-25** — Product name finalized: **Zephyroute** (Zephyr + Route). Chosen from a live-verified shortlist of invented/compound brand names where both `.com` and `.app` were confirmed available via RDAP at check time (`rdap.verisign.com` for `.com`, `pubapi.registry.google/rdap` for `.app`) — `zephyroute.com` and `zephyroute.app` both returned 404 (not registered), reconfirmed twice. Real dictionary words (tributary, inlet, sluice, weir, causeway...) and generic AI-name-generator-style words (fluvia, rivara, orbiq, kyria...) were tried first and found essentially 100% taken on `.com`; only distinctive invented compounds cleared both TLDs. Other cleared candidates not chosen: Stelport, Stelrail, Warpcove, Jettygate — kept here in case Zephyroute becomes unavailable by the time of registration (RDAP availability can change). Prior research artifacts and this PRD's internal project slug (`stellar-intents-gateway`) still refer to the product by its working name; both names refer to the same product.
- **2026-08-25** — Relocated this entire workspace from a throwaway local repo (`~/stellar-intents-gateway/`, created before the correct repo was confirmed) into the project's actual working repository, `https://github.com/JafetCHVDev/stellar-build-only.git` (already cloned locally at `~/projects/stellar-build-only/`), which the user clarified is where this project has been living since the start — chosen specifically because it already has the `stellar-build` skill installed. Added `.stellar-build/bmm/config.yaml` with `project_name: stellar-intents-gateway` so this and future skill runs (`create-architecture`, etc.) resolve consistently in this repo. `{project-root}` and `{doc_workspace}` for this and all future workflow runs on this project are now this repo, not the throwaway one.
- **2026-08-26** — User paused the in-progress `create-architecture` workflow (was mid-way through step-03-starter, an implementation-adjacent decision) to stay at documentation/research depth first, and asked whether stellar-build skills + the SCF Handbook had actually been used to ground this project. Confirmed: yes, `prd`, `create-architecture`, and `advanced-elicitation` are all stellar-build skills and have driven every document produced so far in this repo; the originating idea itself predates this documentation flow (it came from two pre-existing research artifacts the user already had), not from running `find-stellar-idea` within this session. Live-queried the SCF Handbook MCP (not assumed) against the current PRD/architecture content: Near Intents and DeFindex are confirmed valid Integration List building blocks; the tranche #3 "committed onchain metric + measurement window" mechanic matches what Open Question 8 already anticipated. Two real gaps surfaced and added: (1) tranche #2 requires a threat model + monitoring plan neither of which exists yet (Open Question 10); (2) the Integration Track's "register an on-chain footprint at award time" mechanic (Soroban contract IDs / issuing accounts / app-operated wallets) sits in tension with Zephyroute's zero-custody, zero-proprietary-contract design — the address-correlation layer from `architecture.md`'s Integration Coupling Map is the best candidate substitute but isn't yet confirmed acceptable to the panel (Open Question 11, new Risk row in §13). Next: user wants to move into brand/design-system documentation (color palette, brand book) via `create-ux-design`, not code-level starter-template decisions.
- **2026-08-26** — At the user's request, retroactively validated Zephyroute against `find-stellar-idea`'s own offline evidence sources (LumenLoop 728-project catalog, `scf/rounds.json`, a16z/YC/Alliance thesis files) rather than running the skill's actual ideation interview from scratch — user explicitly chose the validation path over regenerating alternative ideas, to avoid destabilizing the completed PRD/architecture/UX-in-progress work. Result written to `addendum.md` §J: the ecosystem-gap claim (nobody else fuses NEAR-Intents-style settlement into a DeFindex deposit) holds up independently against the offline catalog; the $50k–$250k SCF award-band claim is corroborated by the Financial Protocols category's real distribution (median $149,820 across 134 awards); a16z's *Big Ideas 2025* has a directly on-point "chain abstraction / UX-first" thesis item. Also surfaced two figures in addendum §A that don't match this offline dataset (Rozo $248,000 vs. $150,000 here; WOWMAX absent from this dataset entirely) — flagged to re-verify live before either figure is cited in an actual SCF submission, not treated as confirmed either way.
- **2026-08-26** — Created `CLAUDE.md` at the repo root (governs implementation once code starts): bans em dash (—) in all generated content, and 9 acceptance criteria (5 security, 4 quality) derived directly from NFRs/rules already established in the PRD and addendum (non-custodial invariant, no secrets in repo, validate external API responses, review-before-sign, no silent failure, TS strict mode, tests on transaction-building code, no new Soroban contract without a documented Rule #3 check, independent auditability). Note: `prd.md`, `addendum.md`, `architecture.md`, and `ux-design-specification.md` were all written before this rule existed and are full of em dashes — the rule is not retroactive unless the user separately requests a cleanup pass.
- **2026-08-26** — Ran `advanced-elicitation`'s "User Persona Focus Group" method (Priya/UJ-1, Marcus/UJ-2) against the draft Core User Experience section; user approved all 4 resulting changes (first-signature confidence as its own success moment, trustline-detection/FR-3 fork designed explicitly rather than assumed, widget-embedding fidelity as its own experience principle, settlement-wait treated as a first-timer anxiety moment, not just a technical challenge) — applied to `ux-design-specification.md` before it was saved to the document.
- **2026-08-26** — Transferred the `zephyroute` GitHub repo from the personal account (`JafetCHVDev`) to the `ZephyRoute` organization (created earlier by the user via the web UI, since `gh`/the API can't create orgs on github.com) via `POST /repos/JafetCHVDev/zephyroute/transfer`; confirmed live via `gh api orgs/ZephyRoute/repos`. Local `origin` remote updated to `https://github.com/ZephyRoute/zephyroute.git`. All future work in this repo pushes to the org-owned repo, not the personal one.
- **2026-09-05** — Live-queried the SCF Handbook's Soroban Security Audit Bank official rules (not assumed) while working `create-ux-design` Step 7. Finding: the program's eligibility criteria (STRIDE threat model, self-service tooling scan, public disclosure) target projects with a proprietary Soroban contract to audit; Zephyroute holds none by design (Architecture A′), so the program likely does not apply directly, which reinforces rather than undermines the zero-proprietary-contract decision. Its rigor (threat modeling, third-party review, transparent disclosure) is still worth adopting voluntarily at the architecture/security phase, not the UX phase; deferred there rather than acted on now.
- **2026-09-05** — Ran `advanced-elicitation`'s "Security Audit Personas" and "Pre-mortem Analysis" methods against the Step 7 (Defining Core Experience) draft's Experience Mechanics subsection. User approved all resulting hardening: visible on-screen review of destination/amount/minimum before signing, an explicit expired-signature-window state instead of a silent stale resubmission, a transaction hash/explorer link shown alongside the submitted and confirming states, an explicit wallet-disconnect-during-signing state, state reconstruction from Horizon/DeFindex on reload instead of relying on in-memory state, and an explicit on-chain-revert state. One new gap surfaced (insufficient XLM reserve to cover the deposit transaction's fee for a first-time user) was not assumed solved; logged as new PRD Open Question 12 instead.
