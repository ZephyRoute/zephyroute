# Decision Log — PRD: Stellar Intents Gateway

Canonical memory and audit trail for this PRD run. Every decision, change, and override is recorded here as the conversation unfolds.

- **2026-08-25** — Workspace bound at `docs/prds/prd-stellar-intents-gateway-2026-08-25/`. No prior `.stellar-build/bmm/config.yaml` existed in a project; `{project-root}` set to a newly created `~/stellar-intents-gateway/` folder since this is the first artifact of an ongoing project (per user's master rules, this project spans future R&D, not a one-off). `{project_name}` inferred as `stellar-intents-gateway`. Global config's `communication_language: English` overridden for this run — user has consistently written in Spanish throughout prior sessions on this project, so responses continue in Spanish.
- **2026-08-25** — Intent: Create (no existing PRD for this project). Existing inputs identified: (1) prior "Intents Gateway Live Test" artifact (Fase 2, live technical validation of NEAR Intents → Stellar → DeFindex), (2) "Integrate First, Build Last" artifact (Fase 3, re-evaluation against the project's 10 master rules, 13 questions answered, published at https://claude.ai/code/artifact/01a3f145-f24f-4e82-9609-e6ea7c054f95). Both to be extracted as source inputs during Discovery rather than re-derived from scratch.
- **2026-08-25** — Stakes calibration: **Launch**. This is a real Mainnet/SCF Integration Track candidate with real users and capital at stake — PRD written at full depth (Success Metrics, NFRs, Constraints, Risk register, Integration/Dependencies).
- **2026-08-25** — Working mode: **Fast path**. Two subagents (artifact extraction + competitive/market research) ran in parallel during Discovery; three consolidated fast-path questions were then asked (monetization, whether SCF is an explicit v1 goal, launch timeline) rather than a full section-by-section coaching walkthrough.
- **2026-08-25** — Monetization: integrator fee automático de NEAR Intents habilitado por defecto en v1 (no custom billing code). Exact rate unknown — Open Question 9.
- **2026-08-25** — SCF Integration Track application is an explicit primary goal of v1, not just background context — Success Metrics (§9 of prd.md) were written to double as the on-chain metric a panel would ratify for tranche #3.
- **2026-08-25** — Target launch timeline: weeks (aggressive), consistent with Mainnet-From-Day-One — contingent on closing Phase 2's 5 pending confirmations first, now folded into prd.md §14 Open Questions.
- **2026-08-25** — Full draft of `prd.md` and `addendum.md` written from the extraction + research subagent digests plus the 3 fast-path answers. Per Fast-path convention ("you review and we iterate"), the Finalize sequence (decision-log audit, input reconciliation, reviewer gate, polish, close) is deliberately deferred until after the user reviews this draft, rather than run immediately.
- **2026-08-25** — Product name finalized: **Zephyroute** (Zephyr + Route). Chosen from a live-verified shortlist of invented/compound brand names where both `.com` and `.app` were confirmed available via RDAP at check time (`rdap.verisign.com` for `.com`, `pubapi.registry.google/rdap` for `.app`) — `zephyroute.com` and `zephyroute.app` both returned 404 (not registered), reconfirmed twice. Real dictionary words (tributary, inlet, sluice, weir, causeway...) and generic AI-name-generator-style words (fluvia, rivara, orbiq, kyria...) were tried first and found essentially 100% taken on `.com`; only distinctive invented compounds cleared both TLDs. Other cleared candidates not chosen: Stelport, Stelrail, Warpcove, Jettygate — kept here in case Zephyroute becomes unavailable by the time of registration (RDAP availability can change). Prior research artifacts and this PRD's internal project slug (`stellar-intents-gateway`) still refer to the product by its working name; both names refer to the same product.
- **2026-08-25** — Relocated this entire workspace from a throwaway local repo (`~/stellar-intents-gateway/`, created before the correct repo was confirmed) into the project's actual working repository, `https://github.com/JafetCHVDev/stellar-build-only.git` (already cloned locally at `~/projects/stellar-build-only/`), which the user clarified is where this project has been living since the start — chosen specifically because it already has the `stellar-build` skill installed. Added `.stellar-build/bmm/config.yaml` with `project_name: stellar-intents-gateway` so this and future skill runs (`create-architecture`, etc.) resolve consistently in this repo. `{project-root}` and `{doc_workspace}` for this and all future workflow runs on this project are now this repo, not the throwaway one.
- **2026-08-26** — User paused the in-progress `create-architecture` workflow (was mid-way through step-03-starter, an implementation-adjacent decision) to stay at documentation/research depth first, and asked whether stellar-build skills + the SCF Handbook had actually been used to ground this project. Confirmed: yes, `prd`, `create-architecture`, and `advanced-elicitation` are all stellar-build skills and have driven every document produced so far in this repo; the originating idea itself predates this documentation flow (it came from two pre-existing research artifacts the user already had), not from running `find-stellar-idea` within this session. Live-queried the SCF Handbook MCP (not assumed) against the current PRD/architecture content: Near Intents and DeFindex are confirmed valid Integration List building blocks; the tranche #3 "committed onchain metric + measurement window" mechanic matches what Open Question 8 already anticipated. Two real gaps surfaced and added: (1) tranche #2 requires a threat model + monitoring plan neither of which exists yet (Open Question 10); (2) the Integration Track's "register an on-chain footprint at award time" mechanic (Soroban contract IDs / issuing accounts / app-operated wallets) sits in tension with Zephyroute's zero-custody, zero-proprietary-contract design — the address-correlation layer from `architecture.md`'s Integration Coupling Map is the best candidate substitute but isn't yet confirmed acceptable to the panel (Open Question 11, new Risk row in §13). Next: user wants to move into brand/design-system documentation (color palette, brand book) via `create-ux-design`, not code-level starter-template decisions.
- **2026-08-26** — At the user's request, retroactively validated Zephyroute against `find-stellar-idea`'s own offline evidence sources (LumenLoop 728-project catalog, `scf/rounds.json`, a16z/YC/Alliance thesis files) rather than running the skill's actual ideation interview from scratch — user explicitly chose the validation path over regenerating alternative ideas, to avoid destabilizing the completed PRD/architecture/UX-in-progress work. Result written to `addendum.md` §J: the ecosystem-gap claim (nobody else fuses NEAR-Intents-style settlement into a DeFindex deposit) holds up independently against the offline catalog; the $50k–$250k SCF award-band claim is corroborated by the Financial Protocols category's real distribution (median $149,820 across 134 awards); a16z's *Big Ideas 2025* has a directly on-point "chain abstraction / UX-first" thesis item. Also surfaced two figures in addendum §A that don't match this offline dataset (Rozo $248,000 vs. $150,000 here; WOWMAX absent from this dataset entirely) — flagged to re-verify live before either figure is cited in an actual SCF submission, not treated as confirmed either way.
- **2026-08-26** — Created `CLAUDE.md` at the repo root (governs implementation once code starts): bans em dash (—) in all generated content, and 9 acceptance criteria (5 security, 4 quality) derived directly from NFRs/rules already established in the PRD and addendum (non-custodial invariant, no secrets in repo, validate external API responses, review-before-sign, no silent failure, TS strict mode, tests on transaction-building code, no new Soroban contract without a documented Rule #3 check, independent auditability). Note: `prd.md`, `addendum.md`, `architecture.md`, and `ux-design-specification.md` were all written before this rule existed and are full of em dashes — the rule is not retroactive unless the user separately requests a cleanup pass.
- **2026-08-26** — Ran `advanced-elicitation`'s "User Persona Focus Group" method (Priya/UJ-1, Marcus/UJ-2) against the draft Core User Experience section; user approved all 4 resulting changes (first-signature confidence as its own success moment, trustline-detection/FR-3 fork designed explicitly rather than assumed, widget-embedding fidelity as its own experience principle, settlement-wait treated as a first-timer anxiety moment, not just a technical challenge) — applied to `ux-design-specification.md` before it was saved to the document.
- **2026-08-26** — Transferred the `zephyroute` GitHub repo from the personal account (`JafetCHVDev`) to the `ZephyRoute` organization (created earlier by the user via the web UI, since `gh`/the API can't create orgs on github.com) via `POST /repos/JafetCHVDev/zephyroute/transfer`; confirmed live via `gh api orgs/ZephyRoute/repos`. Local `origin` remote updated to `https://github.com/ZephyRoute/zephyroute.git`. All future work in this repo pushes to the org-owned repo, not the personal one.
- **2026-09-05** — Live-queried the SCF Handbook's Soroban Security Audit Bank official rules (not assumed) while working `create-ux-design` Step 7. Finding: the program's eligibility criteria (STRIDE threat model, self-service tooling scan, public disclosure) target projects with a proprietary Soroban contract to audit; Zephyroute holds none by design (Architecture A′), so the program likely does not apply directly, which reinforces rather than undermines the zero-proprietary-contract decision. Its rigor (threat modeling, third-party review, transparent disclosure) is still worth adopting voluntarily at the architecture/security phase, not the UX phase; deferred there rather than acted on now.
- **2026-09-05** — Ran `advanced-elicitation`'s "Security Audit Personas" and "Pre-mortem Analysis" methods against the Step 7 (Defining Core Experience) draft's Experience Mechanics subsection. User approved all resulting hardening: visible on-screen review of destination/amount/minimum before signing, an explicit expired-signature-window state instead of a silent stale resubmission, a transaction hash/explorer link shown alongside the submitted and confirming states, an explicit wallet-disconnect-during-signing state, state reconstruction from Horizon/DeFindex on reload instead of relying on in-memory state, and an explicit on-chain-revert state. One new gap surfaced (insufficient XLM reserve to cover the deposit transaction's fee for a first-time user) was not assumed solved; logged as new PRD Open Question 12 instead.
- **2026-09-13** — Completed the full 14-step `create-ux-design` workflow: locked a dark, Signal Cyan single-accent design system (Fraunces/Public Sans/IBM Plex Mono typography), the Guided Status screen synthesis (persistent progress rail plus a two-depth plain-language quote line), 4 bespoke components, 3 fully mapped user journeys with settlement-failure and signature-window-expiry handling, and a responsive/accessibility strategy with a hard launch-blocking gate on any testing violation. Full detail lives in `ux-design-specification.md`; not re-summarized here.
- **2026-09-13** — Resumed the paused `create-architecture` workflow at Step 3 (Starter Template Evaluation). Selected Next.js 16 (App Router) plus Vercel, with the embeddable widget served via a dedicated iframe route and the correlation layer built as Next.js API routes, verified live against current versions and iframe/wallet-embedding practices rather than assumed. Ran `advanced-elicitation`'s "Security Audit Personas" method against this decision and found a genuine, previously undocumented technical risk: Freighter's extension injection and Stellar Wallets Kit's WalletConnect origin allowlist do not automatically work inside a third-party iframe without explicit configuration on both the THORWallet (CSP `frame-ancestors`) and WalletConnect-project sides. Logged as an addition to PRD Open Question 7, since this extends that open question from a pure partnership question into a technical prerequisite as well.
- **2026-09-13** — Completed `create-architecture` (8/8 steps, 3 real coherence gaps found and fixed in Step 7's validation: state-type flattening, FR-3's missing file assignment, an unspecified nonce protocol) and `create-epics-and-stories` (4/4 steps, 4 epics, 19 stories, a real forward-dependency bug found and fixed during Step 4's own validation: Story 1.12 read a correlation record that Story 3.1 technically wrote first, resolved by moving the write into Stories 1.8/1.11 where the data actually becomes known). User then explicitly reiterated the project's standing "define everything before writing code" instruction, sharpened to require SCF-awareness specifically, and asked to close PRD Open Question 10 (the SCF tranche #2 threat model/monitoring plan requirement) before proceeding toward `dev-story`.
- **2026-09-13** — Wrote `docs/threat-model.md`, a STRIDE analysis built directly from `addendum.md` §C's failure-mode table applied against the actual architecture, resolving PRD Open Question 10. Found five concrete, previously undocumented gaps: `frame-ancestors` needed an explicit partner-origin allowlist rather than a generic policy, no `script-src` CSP directive existed anywhere, CI had no secret-scanning gate, client-facing errors had no stated sanitization rule, and the traction-metrics view (Story 3.2) had no access-control mechanism at all. All five applied directly to `epics.md` as new acceptance criteria, not left as free-floating findings. Also closed PRD Open Question 12 (XLM fee-reserve check) at the story level in the same pass, since it had been flagged during UX design but never actually reflected in a story's acceptance criteria until now.
- **2026-09-20** — User stepped away for one hour, explicitly authorizing autonomous professional decision-making grounded in the SCF Handbook (live-queried, not assumed), with Integration Track named as the current top priority for this project. Re-verified live: NEAR Intents/1Click is confirmed present in the current SCF Integration List under Cross-chain (addendum.md §A.3's flagged re-verification item, now closed). Refreshed Integration Track eligibility and tranche mechanics directly from the Handbook, confirming no drift from what `architecture.md`/`threat-model.md` already assumed (tranche #2 requires the threat model, now satisfied; tranche #3 requires a panel-ratified onchain metric commitment, still Open Question 8). Found the currently active round live: **SCF #46, submission deadline November 8, 2026, up to $150K in XLM**, with the round page itself instructing teams to submit the Interest Form as soon as possible. This is new, time-sensitive information not previously in any document. Proceeding to draft the Interest Form as the next concrete Integration Track deliverable, per the round's own stated urgency; not submitting anything without the user's explicit review, per standing safety rules around sending/publishing on the user's behalf.
- **2026-09-20** — Drafted `docs/scf-interest-form-draft.md` for SCF #46, grounded entirely in already-validated project documents; left the Team field as an explicit, unfabricated placeholder rather than inventing credentials. Caught and fixed a real inconsistency the draft itself introduced: it initially called DeFindex "already-audited," which PRD Open Question 4 explicitly forbids stating publicly before the mainnet bytecode is verified against the audited commit; softened before committing. Made partial live progress on Open Question 4 itself: confirmed OtterSec's March 2025 audit (1 critical, 3 high, all resolved) and found DeFindex's own "Mainnet Deployment 1.0.0" release publishing the exact WASM hashes to check against Stellar.Expert, but could not complete the final live comparison in this pass (Stellar.Expert is a client-rendered page, no browser automation tooling loaded in this session), left honestly marked as partially verified, not closed.
- **2026-09-20** — Significant unplanned finding, verified live: THORWallet already launched its own Stellar integration in November 2025 (NEAR Intents for the swap leg, Blend, not DeFindex, for yield), directly resolving PRD Open Question 7's first half (yes, Stellar is already a destination) but materially weakening the FR12/Epic 4 embed pitch, since THORWallet no longer needs a third party for the core swap-to-Stellar-yield loop they were the target partner for. Updated `addendum.md` §A.4 and PRD Open Question 7 with the finding. Did not decide the strategic reaction (whether to keep pursuing THORWallet, pivot to a different partner, or lean harder on the DeFindex-specific differentiation) since that is a product decision for the user, flagged prominently instead of resolved unilaterally, consistent with how prior significant findings (for example XOXNO) were handled earlier in this project.
- **2026-09-20** — User confirmed the recommended reaction to the THORWallet finding and asked to proceed. Decision recorded: THORWallet deprioritized as the primary Epic 4 target partner (the widget stays generic, a future candidate should specifically lack an existing native swap-to-Stellar-yield loop); the finding is framed as validation of underlying demand, not a threat, in outward-facing material; the DeFindex-specific differentiation (composable, multi-strategy vaults with a built-in rebalance function, verified live against DeFindex's own documentation, versus THORWallet's single-protocol direct Blend access) is now made explicit rather than left implicit. Confirmed this does not touch AC #9/Rule #9 (independent on-chain auditability) at all, since that property is intrinsic to Zephyroute's own architecture, not relative to any competitor; user explicitly asked for this confirmation before proceeding. Applied to `addendum.md` §A.4, PRD Open Question 7 and §12, `epics.md` Epic 4 and its two stories, and `scf-interest-form-draft.md`.
- **2026-09-20** — Checked SCF #46's live submissions list: none received yet as of this pass, the round is still early (Build Submission deadline November 8, 2026; a separate Community Vote phase runs November 23 to December 3, applicable to Open Track submissions, not Integration Track, which stays panel-review-only per the Handbook). No fresh direct-competitor intel to add this pass; worth re-checking closer to the deadline as submissions accumulate.
- **2026-09-20** — Closed most of PRD Open Question 4: found Stellar Expert's public JSON API (`api.stellar.expert/explorer/public/contract/{id}`), which bypasses the client-rendered UI blocker hit earlier. Checked 3 mainnet DeFindex contract addresses directly against the hashes DeFindex itself published (GitHub "Mainnet Deployment 1.0.0" release and their own docs site): all 3 matched exactly (Factory once, the Blend strategy contract at two separate addresses), zero discrepancies. The top-level `defindex_vault` contract itself was not distinctly isolated and checked, so this is real, strong, but not exhaustive verification. Applied to PRD Open Question 4 and `scf-interest-form-draft.md`, both updated to state exactly what was and was not verified, not rounded up to a full "closed" claim.
- **2026-09-20** — Researched PRD Open Question 8 (tranche #3 metric target) against the Handbook. Confirmed the calibration method (modest, credible, measured over an agreed window, tied to a registered onchain footprint the panel can check for gaming) but found no historical numeric NAV/volume examples from past Integration Track awards in the accessible Handbook. Did not invent a target number, the PRD stays honest that this remains genuinely negotiated with the panel, not guessable from documentation alone.
- **2026-09-20** — Resolved PRD Open Question 1, live-verified, a question open since the project's very first documents: both Privy and DFNS confirmed to support Stellar with account creation and trustline operations. DFNS additionally offers Fee Sponsors on Stellar for those same operation types (not for Soroban contract calls, so this does not touch Open Question 12's deposit-fee issue), making it the stronger candidate for Zephyroute's onboarding-friction goal specifically. Updated every stale "unconfirmed" reference across `prd.md` (Open Question 1's own entry, the Risk table row, the Assumptions Index, the FR-6 assumption tag) and `architecture.md` (Technical Constraints, Requirements Coverage Validation). Reframed `epics.md` Stories 2.2 and 2.3: 2.2 (embedded-wallet onboarding) is now the confirmed primary path rather than a conditional one, 2.3 (manual fallback) is retained as a defensive fallback rather than an equally-likely branch. Architecture B stays deferred and unjustified under Rule #3, now with a positive confirmation rather than an open question behind that call.
- **2026-09-20** — Re-verified 1Click's current Terms of Service live for Open Question 5 (KYC/AML). Confirmed the compliance obligation sits with the "Developer" integrator, not the end user, and found a real precedent (Unstoppable Wallet's own 1Click integration) operating without user-level KYC. Explicitly did not treat this as legal resolution, this is supporting evidence for an eventual real legal review, not a substitute for one; the hard gate on "KYC-free" claims stays in place. Added to PRD Open Question 5 with that distinction stated explicitly.
- **2026-09-20** — Consistency pass over `scf-interest-form-draft.md` now that Open Questions 1 and 4 have moved since it was first drafted: added a Stellar Integration bullet for the DFNS embedded-wallet onboarding path (account creation plus trustline setup, fee-sponsored, confirmed live), which was a real, resolved, Integration-Track-relevant building block missing from the draft even though it's now a confirmed primary path in `epics.md` Story 2.2. No other section needed changes, the DeFindex bytecode-verification language already states exactly what was and wasn't checked, and the THORWallet/differentiation framing already reflects the user's confirmed decision.
- **2026-09-20** — Acted on the draft's own open note about "idea stage" reading as a weak signal: pulled the already-existing Phase 2 live route-test evidence from `addendum.md` §E (9 real quotes against the production 1Click API, all 4 in-scope EVM/BTC-to-Stellar routes confirmed working with real correlation IDs and HTTP 201 responses, a confirmed ~$0.30 real technical minimum) into the Current Stage section of `scf-interest-form-draft.md`. This is not new research, it is citing evidence that already existed in the project's own prior validation work but had not yet been surfaced in the submission-facing draft. Removed the now-fulfilled Draft Note that had flagged this gap.
- **2026-09-20** — Closed the WOWMAX/Rozo figure discrepancy flagged back on 2026-08-26 (offline LumenLoop catalog had shown Rozo at $150,000 and no WOWMAX entry at all, versus $248,000 and $98,560 respectively in `addendum.md`'s own table) by querying the SCF directory's own submission records live via stellar-raven, not the offline catalog snapshot. Both figures already in the table were correct: WOWMAX's $98,560 is one exact awarded Build submission (SCF #43); Rozo's $248,000 is the accurate sum of two separately awarded submissions ($98.0K SCF #44, $150.0K SCF #38), not a single-round number, with a third $150.0K submission (SCF #43) correctly excluded as not awarded. Also re-confirmed NEAR Intents/1Click's continued Integration List presence at the same time via the Handbook and updated the stale "not independently re-confirmed" line in §A.3 that had been left unedited after the 2026-09-20 verification pass earlier in this session. No PRD or Interest Form claim needed to change, both were already accurate; this closes an open verification debt rather than fixing an error.
- **2026-09-20** — Added a practical-impact note to PRD Open Question 2 (trustline as a hard settlement precondition): drew the legitimate inference, from facts already verified this session, that Open Question 1's resolution (DFNS creates the account and trustline during onboarding, before any quote or settlement) means the primary user path no longer depends on this question's answer, since the trustline already exists by the time it would matter. The question itself stays genuinely open, since answering it for real requires a non-dry settlement against a fresh, trustline-less account (the same real-money constraint already blocking Open Question 6), which stays out of scope for autonomous work. Scope narrowed explicitly: it now only gates Story 2.3's manual fallback and FR-3's exact precondition phrasing, not FR-6's confirmed primary path.
- **2026-09-20** — Found a genuine staleness gap the earlier THORWallet-repositioning pass had missed: it updated `addendum.md`, `prd.md`, and `epics.md` (Epic 4/Story 4.1), but `architecture.md` and `ux-design-specification.md` still named THORWallet as the concrete, assumed embed partner in roughly 15 places combined, including `ux-design-specification.md`'s UJ-1 (titled "Priya swaps from inside THORWallet," with a Mermaid diagram opening inside THORWallet specifically) and several "Important Gaps"/"Areas for Future Enhancement" lines in `architecture.md` that still described Open Questions 1, 7, and 10 as open even though all three have since resolved. Generalized every reference to partner-agnostic language ("a partner's embed," "the embeddable widget," a renamed UJ-1 title and flowchart), consistent with how the same decision was already applied elsewhere, and corrected the stale Open Question 1/7/10 status lines to reflect their actual current resolution state. Also updated UJ-2's onboarding-fork flowchart labels in `ux-design-specification.md`, which still read "Confirmed by launch / Not confirmed" for the embedded-wallet branch, to reflect DFNS as the confirmed primary path with the manual fallback now framed as the edge case, matching `epics.md` Stories 2.2/2.3. Caught and fixed two em dashes introduced by my own edits mid-pass (this document's no-em-dash rule applies to new content, not the documents' large amount of pre-existing em dashes from before that rule existed).
- **2026-09-20** — Completed the THORWallet staleness sweep by checking `epics.md` itself for the same pattern beyond the Epic 4/Story 4.1 lines already fixed earlier this session: found 5 more stale references (FR12's requirement text, a Project Structure & Boundaries note, UX-DR11's journey description, Story 4.1's AC #4, and Story 4.2's Note framing the CSP/WalletConnect gate as "THORWallet partnership confirmation" specifically). All generalized to partner-agnostic language, with Story 4.2's Note now correctly stating no partner is currently confirmed rather than implying THORWallet's confirmation is still pending. Epic 4's own goal statement and Story 4.2's named-allowlist AC were already correct from the earlier pass and needed no further change. This closes the THORWallet-repositioning propagation across all five affected documents (`addendum.md`, `prd.md`, `epics.md`, `architecture.md`, `ux-design-specification.md`).
